name: store_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  cupertino_icons: ^1.0.8  # تم حذف التكرار
  http: ^1.3.0
  font_awesome_flutter: ^10.8.0
  modal_progress_hud_nsn: ^0.5.1
  firebase_core: ^2.30.0    # النسخة الأحدث من firebase_core
  firebase_auth: ^4.20.0    # النسخة الأحدث من firebase_auth
  flutter_bloc: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
